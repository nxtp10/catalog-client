
import React, { useEffect, useContext, useState } from "react";
import { Route, Routes } from 'react-router-dom';
//import { ProfileForm } from './Profile'
//import { ShopsForm } from './Shops'
//import { TApiCtx, ApiCtx } from "../../api/ApiProvider";
import { Input } from "../../components/Validations";



/*
export function UserAccount(): JSX.Element {
    const shopsForm = ShopsForm()
    const [Message, setMessage] = useState('')

    useEffect(() => {
    }, [])

    function Router() {
        // return(<></>)
        return (
            <>
                <Routes>
                    <Route path='profile' element={<ProfileForm />} />
                    <Route path='shops/*' element={<ShopsForm />} />
                </Routes>
            </>
        )
    }
    return (
        <>
            {Router()}
        </>
    )
}
*/
